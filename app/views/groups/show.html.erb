<div class="group-info">
	<p> <%= @group.name %> </p>
</div>
<div class="users">
	<% @group.users.each do |user| %>
		<div class="user">
			<% if user.first_name == current_user.first_name %>
				<p> You </p>
			<% else %>
				<p> <%= user.first_name %> </p>	
			<% end %>
		</div>
	<% end %>
</div>